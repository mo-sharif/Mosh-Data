import { DefaultImgPipe } from './default-img.pipe';

describe('DefaultImgPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultImgPipe();
    expect(pipe).toBeTruthy();
  });
});
